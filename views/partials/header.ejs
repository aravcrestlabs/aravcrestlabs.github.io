<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AravCrestLabs - <%= title %>
    </title>
    <meta name="description"
        content="Premium jewelry billing software by AravCrestLabs. GemCrest - Modern, offline-first billing solution.">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700;900&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap"
        rel="stylesheet">

    <link rel="stylesheet" href="<%= path %>css/main.css">
    <link rel="stylesheet" href="<%= path %>css/animations.css">

    <!-- Brutalist Libraries -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>
    <script src="https://unpkg.com/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar" id="navbar">
        <div class="navbar-content">
            <a href="<%= path %>index<%= ext %>" class="navbar-logo">AravCrestLabs</a>
            <div class="navbar-nav">
                <a href="<%= path %>index<%= ext %>"
                    class="navbar-link <%= title === 'Home' ? 'active' : '' %>">Home</a>
                <a href="<%= path %>about<%= ext %>"
                    class="navbar-link <%= title === 'About Us' ? 'active' : '' %>">About</a>
                <a href="<%= path %>gemcrest<%= ext %>"
                    class="navbar-link <%= title === 'GemCrest' ? 'active' : '' %>">GemCrest</a>
                <a href="<%= path %>docs/index<%= ext %>" class="navbar-link">Docs</a>
                <a href="<%= path %>contact<%= ext %>"
                    class="navbar-link <%= title === 'Contact' ? 'active' : '' %>">Contact</a>
                <a href="<%= path %>gemcrest<%= ext %>#pricing" class="navbar-cta">Get Started</a>
            </div>
            <button class="navbar-toggle" aria-label="Menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>